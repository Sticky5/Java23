package pl.winiarczyk.lesson4.homework.MinToSecConversion;

class MinutesToSecondsConversion {

    double timeInMinutes;

    public MinutesToSecondsConversion(double minutes) {
        this.timeInMinutes = timeInMinutes;
    }

    public static double convertMinutesToSeconds(double timeInMinutes) {

        double timeInSeconds;
        return timeInSeconds = timeInMinutes * 60;
    }
}
